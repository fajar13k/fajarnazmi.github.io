import React, { Component } from 'react';
import ContactComponent from '../../module/contact/contact';

export default class ContactPage extends Component {
  render() {
    return (
      <ContactComponent />
    );
  }
}